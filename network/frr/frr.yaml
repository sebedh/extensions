name: frr
container:
  security:
    writableRootfs: true
  entrypoint: /sbin/tini
  args:
    - "--"
    - "/docker-start"
  mounts:
    - source: /lib
      destination: /lib
      type: bind
      options:
        - bind
        - ro
    - source: /usr/lib
      destination: /usr/lib
      type: bind
      options:
        - bind
        - ro
    # config via .machine.files
    - source: /var/etc/frr
      destination: /usr/local/etc/frr
      type: bind
      options:
        - bind
        - ro
depends:
  - service: cri
restart: always
