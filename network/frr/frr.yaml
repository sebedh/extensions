name: frr
container:
  security:
    writableRootfs: true
  entrypoint: /sbin/tini
  args:
    - "--"
    - "/usr/local/lib/frr/bgpd"
  mounts:
    - source: /lib
      destination: /lib
      type: bind
      options:
        - bind
        - ro
    - source: /usr/lib
      destination: /usr/lib
      type: bind
      options:
        - bind
        - ro
    # config via .machine.files
    - source: /var/run/frr
      destination: /usr/local/var/run
      type: bind
      options:
        - rshared
        - rbind
        - rw
    - source: /var/etc/frr
      destination: /usr/local/etc/frr
      type: bind
      options:
        - bind
        - ro
depends:
  - service: cri
restart: always
